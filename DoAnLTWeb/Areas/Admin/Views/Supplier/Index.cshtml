
<h2>day la product type</h2>
@model List<DoAnLTWeb.Models.Supplier>

<div class="content-wrapper">
    <!-- Content Header (Page header) -->
    <section class="content-header">
        <!-- Tiêu đề và breadcrumb -->
        <h1>
            Quản lý sản phẩm
        </h1>
        <ol class="breadcrumb">
            <li><a asp-action="Create" asp-controller="Supplier"><i class="fa fa-dashboard"></i>Create</a></li>
        </ol>
    </section>

    <!-- Main content -->
    <section class="content">

        <!-- Default box -->
        <div class="box">
            <div class="box-body">
                <table class="table">
                    <!-- Header của bảng -->
                    <thead>
                        <!-- Các cột -->
                        <tr>
                            <th scope="col">Idsupplier</th>
                            <th scope="col">SupplierName</th>
                            <th scope="col">PhoneSupplier</th>
                            <th scope="col">SupplierAddress</th>
                            <th scope="col">EmailSupplier</th>


                        </tr>
                    </thead>
                    <!-- Dữ liệu sản phẩm -->
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>@item.Idsupplier</td>
                                <td>@item.SupplierName</td>
                                <td>@item.PhoneSupplier</td>
                                <td>@item.SupplierAddress</td>
                                <td>@item.EmailSupplier</td>

                                <td>


                                    <a asp-action="Update" asp-area="admin" asp-controller="Supplier"
                                       asp-route-id="@item.Idsupplier" class="btn btn-sm btn-primary ">
                                        <i class="fa fa-edit"> </i>Edit
                                    </a>


                                    <a href="#" class="btn btn-sm btn-danger"
                                       onclick="confirmDelete('@item.Idsupplier')">
                                        <i class="fa fa-trash"></i> Delete
                                    </a>

                                    <form id="deleteForm_@item.Idsupplier"
                                          action="/admin/Supplier/Delete" method="post" style="display: none;">
                                        <input type="hidden" name="id" value="@item.Idsupplier" />
                                    </form>



                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <!-- Phân trang -->

        </div>
    </section>
</div>






<script>
    function confirmDelete(id) {
        if (confirm('Are you sure you want to delete this product type?')) {
            document.getElementById('deleteForm_' + id).submit();
        }
    }
</script>